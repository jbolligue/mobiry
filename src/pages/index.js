import * as React from "react"
import Layouts from '../components/layout'

const IndexPage = () => {
  return (
    <Layouts>

   <div>add</div>
   </Layouts>
  )
}

export default IndexPage

export const Head = () => <title>Mobiry</title>
